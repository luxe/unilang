package(default_visibility = ["//visibility:public"])

# doesn't build.  needs patched
cc_library(
    name = "tuple",
    hdrs = glob(
        [
            "include/**/*.hpp",
        ],
    ),
    includes = ["include"],
)
