has ncurses menu- trouble auto installing:
mailutils
mysql-server
some true font thing

THESE SHOULD PROBABLY BE REMOVED:
msttcorefonts
mysql-server

#appearance
#--------
#black background
#enable workspaces
#show the menus for a window in the title bar

#brightness and lock
#--------
#turn screen off never
#lock - off
#require my password on wake - off

#language support
#--------
# installs needed stuff since only partially installed by default

# online accounts
#--------
# hooked up gmail

# security and privacy
#--------
#file recording off
#include online search off
#send error reports to Canocial (off)
#send occasional reports to Canocial (off)


#displays
#--------
#small resolution
#scaled menu title bars to 0.75
#turned off sticky edges


#keyboard
key press delay low
keypress speed high
turn off cursor blink
shortcuts - come back to

#mouse settings changed
#sound can go over 100
#backups set to off

#used ubuntu tweak
#set pro fonts

#software installed
ubuntu after install
	restricted extras
	google chrome
	vlc
	gimp
	pdf tools

#switch to gnome (metacity)
#customize panels
#turn off backup copy for gedit

sudo apt-get update --fix-missing




--------------------



#!/usr/bin/env bash


#exit;

#set files
#DEVELOPMENT_LIST="$dir/data/development.list"

#apt-get --no-install-recommends


#gnome
#sublime
#google chrome

#aptitude safe-upgrade


#some initial apt-get stuff
#Run_Command_And_Exit_If_Command_Failed "checking for broken deps" "apt-get -y check"

#allow bitmap fonts
#Run_Command_And_Exit_If_Command_Failed "removing bitmap font lock" "rm /etc/fonts/conf.d/70-no-bitmaps.conf"
#Run_Command_And_Exit_If_Command_Failed "allowing bitmap fonts" "ln -s /etc/fonts/conf.avail/70-yes-bitmaps.conf /etc/fonts/conf.d/70-yes-bitmaps.conf"



#fix breadcrumb
#gconftool --type bool --set /apps/nautilus/preferences/pathbar_like_breadcrumbs true
#touch_dir ~/.themes/nautilus
#copy_dir_contents_to ./home-folder-settings/.themes/nautilus ~/.themes/nautilus
#nautilus -q


#echo "Showing all autostart in Startup Applications";
#sudo sed -i 's/NoDisplay=true/NoDisplay=false/g' /etc/xdg/autostart/*.desktop

# See all desktop items after converting back to gnome (metacity)
# gsettings set org.gnome.desktop.background show-desktop-icons true
