
c++ question:
array decay - use array_view
range errors - use array_view

Example: don't:
void read(int* p, int n);       // read max n integers into *p
Example:

void read(array_view<int> r);   // read into the range of integers r




programming words I don't know:
Lenses = generic getter/setter pair
Laarhoven version of lenses follows Toneda lemma or, even more directly Yoneda embedding
prisms.  product types.
functor-polymorphic functions
profunctor-polumorphic isomorphisms
Yoneda lemma
Isomorpphisms


what is an array_view<T> and how do I use it?
