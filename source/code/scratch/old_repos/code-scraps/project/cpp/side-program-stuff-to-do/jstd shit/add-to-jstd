#include <iostream>
#include <vector>
#include <algorithm>

std::vector<int> Create_An_N_Sized_Vector_With_Iota_Starting_At_M(int size, int start);
std::string Get_Back_Duplicate_Characters(std::string str);

Make_Horizontal_Vector_Vertical();
Make_Vertical_Vector_Horizontal();

int main(){
    
    auto list = Create_Iota_Vector(2,10);
    for (i: list) std::cout << i << ' ';
}

std::vector<int> Create_An_N_Sized_Vector_With_Iota_Starting_At_M(int size, int start){
    std::vector<int> v(size);
    std::iota(v.begin(),v.end(),start);
    return v;
}


std::string Get_Back_Duplicate_Characters(std::string str){
return "";
}
