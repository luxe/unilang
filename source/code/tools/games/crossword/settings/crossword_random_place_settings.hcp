class
❪vector❫
❪string❫
⚯

⚞⚟

◀public:

    //for logging
    bool show_word_being_fitted;
    
    //to increase the likelyhood of a passthrough occurring,
    //we can attempt n number of times to randomly place a word in hopes of a passthrough
    //if passthrough occurs, we can keep it, otherwise we fallback to the first random placement that fits (regardless of passthrough)
    int primed_passthrough_attempts;
▶
