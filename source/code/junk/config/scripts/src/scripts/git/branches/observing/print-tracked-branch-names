#!/usr/bin/env bash
. $HOME/.bin/scripts/project/src/utility_functions/bootstrap; #
#-----------------------------------------#

#we cut off the first 3 chars because
# git puts spaces and a * next to the current branch
git branch | cut -c3-;
