class
❪vector❫
❪string❫
⚯

⚞⚟

◀
public:
    
    //whether or not a dockerfile already exists at the root of the repo.
    //if it does, we likely do not need to conjure one up and create it on the root ourselves
    bool ፠has_dockerfile_on_root፠ = false;
    
    //assuming we need to create our own dockerfile,
    //this is the content of what should go inside
    std::string ፠custom_dockerfile_content፠;
    
▶