class
⚯
⚞⚟

◀public: static▶ std::string ☀Run(std::string const& url, std::string const& cookie)❰
    
    std::string command;
    command = "curl ";
    command += url;
    command += " ";
    command += "-H 'cookie: ";
    command += cookie;
    command += "'";
    
    std::cout << command << std::endl;
    auto results = execute(command);
    return results;
❱