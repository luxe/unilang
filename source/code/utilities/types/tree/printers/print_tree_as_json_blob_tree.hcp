class
⚯
⚞⚟

◀public: 

template <typename T>
static void Print(tree<T> const& tr){

    for (auto it = tr.begin(); it != tr.end(); ++it){
        auto depth = tr.depth(it);
        T obj = *it;
        std::cout << Create_String_Of_N_Strs(depth," ");
        std::cout << "- ";
        std::cout << Type_To_Json_String::As_Minified_JSON_String(obj) << std::endl;
    }
}






▶




