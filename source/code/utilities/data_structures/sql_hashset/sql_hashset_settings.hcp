class
⚯
⚞⚟

◀public:

  //file name related
  //if true, the name is ignored
  std::string ፠name፠;
  bool ፠make_unique_name፠ = false;
  
  //the file to load each line into the database
  //loading of the file is only done if the database was not persisted
  std::string ፠load_file፠;
  
  //should the file be deleted in the destructor?
  //PERSISTENT will keep the database around even when the object goes out of scope
  Persistence ፠file_persistence፠ = Persistence::PERSISTENT;
▶ 
