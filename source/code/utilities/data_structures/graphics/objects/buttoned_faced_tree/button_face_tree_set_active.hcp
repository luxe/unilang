class
⚯
⚞⚟


◀public: static▶ void ☀Set_Active(Button_Face_Tree const& tree, bool const& val)❰

    for (auto it = tree.nodes.begin(); it != tree.nodes.end(); ++it){
        it->label.state.faces[it->label.state.index].label.user_state.is_active = val;
    }
    
❱