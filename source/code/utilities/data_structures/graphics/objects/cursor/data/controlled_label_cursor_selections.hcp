class
❪"code/utilities/data_structures/graphics/objects/cursor/data/label_cursor_selection"❫
❪"code/utilities/data_structures/graphics/objects/cursor/data/cursor_ability"❫
⚯

⚞⚟

◀public:
    
    //what the cursors have permission to do
    //for example, a user might want to edit text, but the cursor disallows this
    Cursor_Ability ፠ability፠;
    
    // It is possible to have more than 1 cursor(think of sublime and other editors).
    // If the textbox is not focused, it probably won't have any cursors
    std::vector<Label_Cursor_Selection> ፠cursors፠;
▶

