class
❪string❫
❪"code/utilities/formats/json/extraction/json_extraction_settings"❫
⚯

⚞⚟

◀
public:
    
    // per the spec (decl & def)
    std::string ፠id፠;
    
    // per the spec (decl)
    std::string ፠title፠;
    
    // per the spec (decl)
    std::string ፠type፠;
    
    // per the spec (decl)
    std::string ፠default_value፠;
    
    // per the spec (def)
    std::string ፠value፠;

▶