class
⚯

⚞⚟

◀
public:
    
    // per the spec (decl & def)
    std::string ፠id፠;
    
    // per the spec (decl)
    std::string ፠title፠;
    
    // per the spec (decl)
    std::string ፠type፠;
    
    // per the spec (decl)
    std::string ፠default_value፠;
    
    // per the spec (def)
    std::string ፠value፠;
    
    // Custom logic for populating the value based on a json blob
    Json_Extraction_Settings ፠extract፠;

▶