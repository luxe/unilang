class
❪"code/utilities/data_structures/position/position"❫
⚯

⚞⚟

◀public:
    
    //this where the cursor is actually located as an index into the character array of the message
    //-1 is valid but message.size() is not.
    int label_index = 0;
    
    //negative is backwards / positive is forward
    int additional_chars_selected = 0;
    
    //this is all for show.
    //its the location where the cursor is drawn.
    //make sure this is updated along with any manipulations to label_index
    Position recommended_cursor_pos;
▶

