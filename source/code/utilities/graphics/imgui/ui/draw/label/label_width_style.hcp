class
❪vector❫
⚯

⚞⚟

◀public:
    
    // if we are growing the width, then
    //  we will not need to use the fixed width
    bool grow_width = true;
    
    int fixed_width = 100;
    int cycle_offset = 0;
    int cycle_offset_pixel_jump = 1;
▶

