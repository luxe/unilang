class
❪string❫
⚯

⚞⚟

◀public:▶ ☀◈(std::string const& exporter, std::string const& language, std::string const& style)◖
              : exporter(exporter)
              , language(language)
              , style(style)
             ◗❰❱
             
◀public:
    std::string exporter;
    std::string language;
    std::string dialect;
    std::string style;
    bool run_local_programs = true;
    bool transfer = true;
    bool run_remote_programs = true;
    bool build = true;
▶
