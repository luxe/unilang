#!/usr/bin/env zsh

# make sure ice cream is setup for remote caching
#told not to run start.  but restart should also work if its stopped
#sudo service icecc-scheduler start;
sudo service iceccd restart;

#this should print:
# * iceccd is running
sudo service iceccd status;