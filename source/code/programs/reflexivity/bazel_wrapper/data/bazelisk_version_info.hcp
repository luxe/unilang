class
⚯

⚞⚟

◀public:

    //properties for getting the bazel binary from their github release page
    std::string bazel_version;
    std::string os;
    std::string arch;
    std::string sha256;
    
    //properties for building bazel from source.
    //this is to allow using unreleased or forked versions of bazel
    std::string direct_url;
    std::string commit;
▶
