kind: pipeline
name: default

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker build -t unilang .
  - docker run unilang ./ci_scripts/drone

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
